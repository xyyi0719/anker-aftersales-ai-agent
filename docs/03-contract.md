# （已废止）三层接口契约 v1

> **本文已作废，请勿再改。** 契约的唯一真源是 [`docs/V2/spec/00-契约冻结.md`](V2/spec/00-契约冻结.md)。

本文件是 2026-09-21 之前的 v1 契约草案，当时自标「状态：待实现」。它描述的目标此后已在代码中实现，
但字段名、阈值与现象枚举在实现过程中都改过——而改动只写进了 `00-契约冻结.md`，本文没有同步：

| 项 | 本文（v1，已过时） | 代码与冻结契约（现行） |
|---|---|---|
| 现象字段 | `vision.phenomenon` | `vision.fault_phenomenon` |
| 现象取值 | 英文五值，如 `screen_dark` / `swelling` | 中文十四值，如 `屏幕异常` / `鼓包` |
| 视觉跳级阈值 | 前端 0.75 | 三端统一 `0.8`（`VisionInspector.tsx:14`） |
| 型号来源 | 「只用于比较，不用于设定 product」 | 文字没给型号时由图片识别结果设定（`engine.py:142`） |
| 视觉用例通过率 | 「视觉 2/12 ❌」 | `scripts/eval_metrics.py` 实测看图行为 12/12 |

两份契约并存时，按本文改代码会把三端改歪（`docs/V2/spec/README.md` 遗留问题第 6 条记录的就是这个风险）。
保留本文件只是为了不给旧链接留下断链。

| 要查什么 | 去哪 |
|---|---|
| 契约 A（extract 产出） | `docs/V2/spec/00-契约冻结.md` § 契约 A |
| 契约 B（state 产出，前端消费） | 同上 § 契约 B |
| 契约 C（前端渲染依赖与取值纪律） | 同上 § 契约 C |
| 机器校验 | `scripts/check_contract.py`（不一致即失败） |
| 契约测试 | `tests/test_contract.py` |
